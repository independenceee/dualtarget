import React from "react";
import classNames from "classnames/bind";
import styles from "./Djed.module.scss";
const cx = classNames.bind(styles);

type Props = {};

const Djed = function ({}: Props) {
    return (
        <div className={cx("wrapper")}>
            <div className={cx("inner")}>
                
            </div>
        </div>
    );
};

export default Djed;
